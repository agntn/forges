---
title: Agents
icon: i-lucide-bot
description: The same 49 tools over MCP, the Pi extension and the OMP extension, what they hold back, and how tokens and hosts stay out of a model's hands.
---

## 49 tools, three surfaces

The MCP server, the Pi extension and the OMP extension all call the same executors in `src/tool-operations.ts`, so they answer identically. Fix one, fixed everywhere. The hosted tools mirror the ten resources; local merge verification reads a checkout:

| Group                  | Tools                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| repositories           | `forges_repos_list`, `forges_repos_get`, `forges_repos_contents`                                                                                                                                                                                                                                                                                                                                                                          |
| contribution templates | `forges_contribution_templates_list`, `forges_contribution_templates_get`                                                                                                                                                                                                                                                                                                                                                                 |
| code                   | `forges_code_search`                                                                                                                                                                                                                                                                                                                                                                                                                      |
| CI and commits         | `forges_ci_runs_list`, `forges_ci_jobs_list`, `forges_ci_jobs_log`, `forges_commits_search`, `forges_commits_list`, `forges_commits_compare`, `forges_commits_get`, `forges_commits_patch`                                                                                                                                                                                                                                                |
| releases               | `forges_releases_list`, `forges_releases_get`, `forges_releases_create`, `forges_releases_update`                                                                                                                                                                                                                                                                                                                                         |
| issues                 | `forges_issues_list`, `forges_issues_search`, `forges_issues_get`, `forges_issues_comments`, `forges_issues_comments_get`, `forges_issues_comments_create`, `forges_issues_create`, `forges_issues_update`                                                                                                                                                                                                                                |
| pull requests          | `forges_pull_requests_list`, `forges_pull_requests_search`, `forges_pull_requests_search_global`, `forges_pull_requests_get`, `forges_pull_requests_files`, `forges_pull_requests_checks`, `forges_pull_requests_reviews`, `forges_pull_requests_reviews_get`, `forges_pull_requests_comments`, `forges_pull_requests_comments_get`, `forges_pull_requests_comments_create`, `forges_pull_requests_create`, `forges_pull_requests_update` |
| users and auth         | `forges_users_get`, `forges_users_authenticated`, `forges_auth_reload`                                                                                                                                                                                                                                                                                                                                                                    |
| review threads         | `forges_threads_list`, `forges_threads_get`, `forges_threads_reply`, `forges_threads_resolve`, `forges_threads_unresolve`                                                                                                                                                                                                                                                                                                                 |
| local inspection       | `forges_local_inspect`                                                                                                                                                                                                                                                                                                                                                                                                                    |
| local Git              | `forges_local_merge_verify`                                                                                                                                                                                                                                                                                                                                                                                                               |

Every hosted tool takes a `platform` of `github`, `gitlab` or `gitea` plus the owner, repo, number, tag or username the call needs. Both of the first two have shortcuts, because models kept tripping over them. `platform` can be left out and means `github`. A repository can arrive as the slug everyone already writes: `{ "repo": "agntn/keys" }` reads the same repository as `{ "owner": "agntn", "repo": "keys" }`. An `owner` passed next to a slashed `repo` is fine when it repeats the slug's owner, in any case. One that names someone else is contradictory, so it fails instead of one of the two winning quietly, and so does a `repo` with two slashes. `forges_repos_list` still wants its `owner`, it has no repo to carry one.

## Checks that finish

`forges_pull_requests_checks` reads the checks of a pull request as they are right now. That is rarely the question. A model that wants to know whether CI went green calls it again, and again, and every answer is "still running".

Pass `waitSeconds` instead, 1 to 300. The call comes back when every check of the pull request has concluded, or when the budget runs out. You get the same page as before plus a `wait` field: `settled`, `waitedMs`, `polls` and the names still `pending`. A timeout is an ordinary result, not an error, so the state you asked for is still there. A pull request with no checks answers at once rather than sitting out the budget.

Settlement is judged over every page of checks, not over the page you asked for. A green first page while page two is still building is not a finished run.

## Local inspection

`forges_local_inspect` returns local status, tracked files and recent HEAD commit messages, including bodies. Pass `cwd`, optional `paths` (Git pathspecs relative to the repository root) and `historyLimit` (1-100, default 3). For example, `paths: ["*AGENTS.md"]` finds tracked instruction files; `:(literal)` treats wildcard characters literally. Omit `paths` to inspect the whole repository. The library entry is `inspectLocal` from `@agntn/forges/local`.

Status rows and tracked paths are paged separately. `statusLimit` and `filesLimit` are 1-1000 (default 1000), and a page also stops at 64 KiB of path bytes. Pass `nextStatusOffset` back as `statusOffset` and `nextFilesOffset` as `filesOffset`, keeping `paths` unchanged, until it's `null`. Need only more status? Set `filesLimit: 1`. Each call reads Git again, so don't change the checkout while collecting pages. History isn't paged and keeps its 1 MiB subprocess limit. Every Git command still has a ten-second timeout. A path larger than 64 KiB is an error, not a truncated name.

Status keeps the index and worktree columns separate, including untracked files and rename source paths. An unborn branch returns `headSha: null` and no commits. Nothing is fetched or written, but concurrent edits can change the checkout between reads. Git must be on `PATH` and support both `--no-lazy-fetch` and `ls-files --deduplicate`.

## Local merge verification

`forges_local_merge_verify` takes `cwd`, `head`, `mergeCommit`, `target` and `paths`. It runs Git locally, needs no platform credentials and returns `platform: "local"`. Fetch the target first, then pass the forge's merge or squash commit and the PR head. The library entry is `verifyLocalMerge` from `@agntn/forges/local`.

The result pins all three resolved SHAs. `mergeReachable` checks the merge commit against the target's local history; `pathsMatch` compares the head and merge trees, including file modes. Paths are literal and relative to the checkout root, even when `cwd` is a subdirectory. Supply 1 to 100 files or directories. Each must exist in the head tree, merge tree or the merge commit's first parent, so a file deleted from both compared trees can still be checked. In a shallow checkout, fetch that parent before checking such deletions. Globs aren't expanded.

No fetch, checkout, reset, branch deletion or credential change happens here. Inherited `GIT_*` variables can't redirect the checkout, replacement objects are disabled, and diffs don't run external drivers or text conversion. Each Git command has a ten-second timeout and a 1 MiB output limit. Invalid refs, absent paths and command failures throw rather than returning false.

A match isn't a cleanup verdict. Omitted paths aren't checked, a shallow checkout may lack ancestry, and refs can move after the read. Confirm the live PR and local worktree state before any cleanup; this tool doesn't do that for the caller.

## MCP

```bash
forges mcp
claude mcp add forges --scope user -- npx -y @agntn/forges mcp
```

Or in a client's config:

```json
{
  "mcpServers": {
    "forges": { "command": "npx", "args": ["-y", "@agntn/forges", "mcp"] }
  }
}
```

The server speaks MCP over stdio. `createMcpServer()` from `@agntn/forges/mcp` returns the same server for a host that brings its own transport.

An MCP client sees the text a tool returns and nothing else, so the text carries the whole answer as JSON. Lists and searches drop bodies and name the tool that reads one in full. One page of a busy repository with bodies is big enough to push the conversation that asked for it out of the window, and I would rather the model asks twice. Pull request search leaves revision details to `forges_pull_requests_get`. `forges_threads_list` bounds each comment to twelve lines and four thousand characters but keeps every comment of every thread on the page, so ask for a small `perPage` on a heavily reviewed pull request. The comment tools carry the same bound, and their `_get` variants read one comment whole.

## Reads, writes and credentials

Hosted read tools use the normal [token chain](/guide/auth) and fall back to anonymous access when nothing is found. Anonymous providers are kept apart and can never be reused for a write, not even by accident.

Eleven tools write: `forges_issues_create`, `forges_issues_update`, `forges_issues_comments_create`, `forges_pull_requests_create`, `forges_pull_requests_comments_create`, `forges_pull_requests_update`, `forges_releases_create`, `forges_releases_update`, `forges_threads_reply`, `forges_threads_resolve` and `forges_threads_unresolve`. They carry MCP annotations saying so, `readOnlyHint: false`. The five creates and the reply are marked as not idempotent because doing them twice leaves two of them behind. Resolve, unresolve and the three updates are idempotent, and the three updates are the tools marked destructive, because the title and text they replace are gone. `forges_users_authenticated` names the account the writes would go out as, check it before you let a model write anything.

The credential stays pinned per platform and endpoint until `forges_auth_reload` swaps it and returns the new profile. Reload is gated as a mutation because it changes local server state. It writes nothing to the platform.

With two GitHub logins in `gh`, a write can name the one it goes out as. `account: "octocat"` on any of the eleven reads that login's token with `gh auth token --user octocat`, asks `/user` whose it is, and refuses the write when the answer is someone else. The check is the whole point: a `GH_TOKEN` in the environment still wins the lookup, and a reply signed by the wrong person looks exactly like a success. The active `gh` account stays where it was, and Pi's confirmation dialog names the account too. `forges_users_authenticated` and `forges_auth_reload` take the same field. GitLab and Gitea reject it, neither CLI hands out a token for a login that isn't the active one.

## Hosts stay local

A self hosted endpoint is `FORGES_GITHUB_BASE_URL`, `FORGES_GITLAB_BASE_URL` or `FORGES_GITEA_BASE_URL` in the agent's process environment. Never a tool argument, so a model cannot aim a call at another host. A failure names the status and, on a rate limit, the retry window. It never repeats the endpoint, so the host stays out of the model's context even when the platform answers with an error.

A failed operation is a tool error, not a transport failure: unknown repository, rejected token, exhausted rate limit. The model sees a sentence, the session survives.

## Pi and OMP

```bash
pi install npm:@agntn/forges
```

The package declares both extensions in `package.json`. They render tool calls and results in the terminal: a repository row, a list of pull requests with their states, a thread with its comments, bounded the same way the MCP text is. In Pi, `forges_pull_requests_create`, `forges_pull_requests_update`, `forges_releases_create` and `forges_releases_update` show what they are about to send and wait for a confirmation, and refuse to run without a UI to ask. `forges_repos_get` includes fork state, the parent and the viewer's role. A `null` role means the platform said nothing, not that the viewer has no access.

## What the answer is

The tools return the normalized objects, not prose. The [Explorer](/explorer) shows the same objects for any public repository, a quick way to check a model's summary against the source when it gets creative.

::caution
Issue bodies, comments and review threads are written by whoever has an account on the platform. Data to report, not instructions to follow. An agent that reads `body: "ignore previous instructions"` has learned one thing about the issue and nothing about what to do next.
::
